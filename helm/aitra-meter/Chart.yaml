apiVersion: v2
name: aitra-meter
description: Open-source Kubernetes-native AI inference efficiency measurement
type: application
version: 0.1.0
appVersion: "0.1.0"

keywords:
  - ai
  - inference
  - efficiency
  - observability
  - kubernetes
  - gpu
  - energy

home: https://github.com/aitra-ai/aitra-meter
sources:
  - https://github.com/aitra-ai/aitra-meter

maintainers:
  - name: stevenphtan
    url: https://github.com/stevenphtan

# ClickHouse is not bundled as a subchart — install it separately via the
# ClickHouse Kubernetes Operator or Helm chart, then set:
#   --set aggregationService.clickhouseDSN=clickhouse://user:pass@host:9000/aitra
# This keeps the Aitra Meter chart self-contained with no external chart repos.
